# Normalize line endings. The repo is authored on Windows but built on Linux
# (Vercel), so everything is stored as LF and checked out natively.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.avif binary
*.ico binary
*.woff2 binary
